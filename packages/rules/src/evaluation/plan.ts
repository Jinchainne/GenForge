import type { EvaluationPlan } from "@genforge/domain";

export function createDefaultEvaluationPlan(): EvaluationPlan {
  return {
    version: "1.0.0",
    requiredStages: [
      "intake",
      "repository_fetch",
      "evidence_build",
      "mandatory_checks",
      "manual_review_gate",
      "decision_gate",
      "reporting",
    ],
    mandatoryChecks: [
      {
        id: "check-contract-presence",
        stage: "mandatory_checks",
        requiredRuleId: "GL-GATE-001",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-genlayer-branding-only",
        stage: "mandatory_checks",
        requiredRuleId: "GL-GATE-002",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-contract-source-clarity",
        stage: "mandatory_checks",
        requiredRuleId: "GL-GATE-006",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["manual_review_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-deployment-match",
        stage: "mandatory_checks",
        requiredRuleId: "GL-GATE-007",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["manual_review_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-shape-only-validator",
        stage: "mandatory_checks",
        requiredRuleId: "GL-CON-003",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-simulated-wallet",
        stage: "mandatory_checks",
        requiredRuleId: "GL-FE-001",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-real-contract-calls",
        stage: "mandatory_checks",
        requiredRuleId: "GL-FE-002",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-address-consistency",
        stage: "mandatory_checks",
        requiredRuleId: "GL-FE-005",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["manual_review_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-reproducibility",
        stage: "mandatory_checks",
        requiredRuleId: "GL-ENG-001",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
      {
        id: "check-pinned-dependencies",
        stage: "mandatory_checks",
        requiredRuleId: "GL-ENG-002",
        requiredEventKind: "rule_evaluated",
        mustOccurAfter: ["evidence_build"],
        mustOccurBefore: ["decision_gate", "scoring"],
        blocksScoringUntilComplete: true,
      },
    ],
    scoringRequiresStages: [
      "mandatory_checks",
      "manual_review_gate",
      "decision_gate",
    ],
    scoringAllowedDecisions: ["ACCEPT_FOR_SCORING"],
  };
}
